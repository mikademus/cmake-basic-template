cmake_minimum_required(VERSION 3.10)

# Top-level project
project(MyWorkspace)

# Add subdirectories for SharedLib and ProjectA
add_subdirectory(SharedLib)
add_subdirectory(ProjectA)
add_subdirectory(ProjectB)
